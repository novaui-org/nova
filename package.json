{
  "name": "root",
  "private": true,
  "scripts": {
    "dev:components": "lerna watch --scope=\"nova\" -- lerna run build --scope=\"nova\"",
    "dev:docs": "lerna run dev --scope=\"docs\"",
    "dev": "npm-run-all --parallel dev:*"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^7.3.1",
    "npm-run-all": "^4.1.5"
  }
}
