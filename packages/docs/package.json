{
  "name": "@nova-org/docs",
  "version": "0.0.1-next.0",
  "description": "",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "release": "release-it --ci --config ./release-it.json --git.tagExclude='*[-]*'",
    "release:next": "release-it --ci --config ./release-it.json --preRelease=next"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.75.0",
    "vitepress": "^1.1.0"
  },
  "dependencies": {
    "@nova-org/components": "^0.0.1-next.0",
    "@nova-org/icons": "^0.0.1-next.0",
    "@oku-ui/motion-nuxt": "^0.2.1",
    "motion": "^10.16.4"
  },
  "peerDependencies": {
    "vue": "^3.4.21"
  },
  "nx": {
    "targets": {
      "dev": {
        "dependsOn": [
          "build"
        ]
      }
    }
  }
}
